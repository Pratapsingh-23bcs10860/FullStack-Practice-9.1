import React from "react";

function App() {
  return (
    <div style={{ textAlign: "center", marginTop: "50px" }}>
      <h1>🚀 React App Dockerized with Multi-Stage Build</h1>
      <p>This React app is served via Nginx from a lightweight Docker image!</p>
    </div>
  );
}

export default App;
